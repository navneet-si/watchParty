version: '3.8'
services:
  sync-server:
    build:
      context: ./extensionServer
      dockerfile: Dockerfile
    container_name: webrtc-sync-server
    ports:
      - "3000:3000"
    restart: always
